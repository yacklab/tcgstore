// xs, extra-small: 0px
// sm, small: 600px
// md, medium: 960px
// lg, large: 1280px
// xl, extra-large: 1920px

export default function() {
  return 40; // until pagination is in url
  // if (!window) return 50;
  // const innerWidth = window.innerWidth;
  // if (innerWidth < 600) return 10;
  // if (innerWidth < 960) return 30;
  // return 60;
}
